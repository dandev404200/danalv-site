# Frontend Environment Variables
# Copy this file to .env.local for development

# ===========================================
# DEVELOPMENT ONLY
# ===========================================

# API Base URL - only needed when running backend separately during development
# In production, frontend and backend share the same origin, so this is not needed
# (requests use relative paths like /api/digest)
VITE_API_BASE=http://localhost:8000
